# RISC-V模型机实验报告

## 主要内容

### 一、模型机指令设计

RISC-V是一个模块化的指令集架构（ISA），它的核心是RV32I，最基础的ISA。RV32I的指令可以分成六种格式，分别是:用于寄存器-寄存器操作的 R 类型指令,用于短立即数和访存 load 操作的 I 型指令,用于访存 store 操作的 S 型指令,用于条件跳转操作的 B 类型指令,用于长立即数的 U 型指令和用于无条件跳转的 J 型指令。本次实验实现了R、I、S、B类型中的部分指令。

### 二、测试程序设计

// test.hex - 测试所有支持的指令
// 格式：[地址] 指令码  // 汇编指令  # 注释

// ===== 1. R型指令测试 =====
00000093  // addi x1, x0, 0     # x1 = 0 (初始化)
00100113  // addi x2, x0, 1     # x2 = 1 (初始化)
002081B3  // add  x3, x1, x2    # x3 = x1 + x2 = 1 (加法测试)
403100B3  // sub  x1, x2, x3    # x1 = x2 - x3 = 0 (减法测试)
003170B3  // and  x1, x2, x3    # x1 = x2 & x3 = 1 (AND测试)
003160B3  // or   x1, x2, x3    # x1 = x2 | x3 = 1 (OR测试)
003140B3  // xor  x1, x2, x3    # x1 = x2 ^ x3 = 0 (XOR测试)
003120B3  // slt  x1, x2, x3    # x1 = (x2 < x3) ? 1 : 0 = 0 (有符号比较测试)

// ===== 2. I型指令测试 =====
00C00193  // addi x3, x0, 12    # x3 = 12 (立即数加法)
FF718213  // addi x4, x3, -9    # x4 = x3 + (-9) = 3 (负立即数测试)
0021F293  // andi x5, x3, 2     # x5 = x3 & 2 = 0 (ANDI测试)
0021E313  // ori  x6, x3, 2     # x6 = x3 | 2 = 14 (ORI测试)
0021C393  // xori x7, x3, 2     # x7 = x3 ^ 2 = 14 (XORI测试)
0021A413  // slti x8, x3, 2     # x8 = (x3 < 2) ? 1 : 0 = 0 (SLTI测试)

// ===== 3. 内存访问指令测试 =====   S
00002223  // sw   x0, 4(x0)     # MEM[4] = x0 = 0 (存储字测试)
00402083  // lw   x1, 4(x0)     # x1 = MEM[4] = 0 (加载字测试)

// ===== 4. 分支指令测试 =====  B
00208463  // beq  x1, x2, 8     # if x1==x2 pc+=8 (不跳转，x1=0, x2=1)
00008463  // beq  x1, x0, 8     # if x1==x0 pc+=8 (跳转，x1=0, x0=0)
0020C463  // blt  x1, x2, 8     # if x1<x2 pc+=8 (跳转，x1=0<1=x2)

0xfe100ce3 //beq x0, x1, -8

//// ===== 循环测试 =====

//fe019ee3  // bne  x3, x0, -4    # 如果 x3 != 0，跳转到递减指令 (实现循环)

### 三、对模型机的改进

便于扩展

增加指令

提高性能

## OT

### 一、目的与任务

学习RISC-V指令集架构设计思想，完成32位模型机设计。使用verilog完成模型机电路设计，使用verilator完成电路综合和仿真，编写测试程序完成所有指令的测试。

### 二、相关技术简介

#### 1. **RISC-V 指令集架构**

RISC-V 是一种**开源、精简指令集（RISC）** 的处理器架构，由加州大学伯克利分校于2010年发布。其核心优势包括：

- **模块化设计**：基础整数指令集（RV32I）仅40条指令，支持可扩展模块（如乘法指令集M、原子操作指令集A）
- **开放生态**：免版税许可，推动学术与工业界广泛应用（如IoT、AI加速器、高性能CPU）
- **精简高效**：固定长度指令（32位），规整的指令格式（仅6种编码格式），简化硬件实现
  本实验基于 **RV32I 基础指令集**实现模型机，支持算术运算（ADD/SUB）、逻辑运算（AND/OR）、内存访问（LW/SW）和控制流（BEQ/JAL）等核心指令。

#### 2. **Verilog 硬件描述语言**

Verilog 是**数字电路设计的工业标准语言**，在本实验中用于实现RISC-V模型机的硬件结构：

- **核心组件建模**：
  - 寄存器堆（32×32位）
  - ALU（支持12种算术逻辑运算）
  - 控制单元（生成10+控制信号）
  - 分级流水线（IF/ID/EX/MEM/WB）
- **关键特性**：
  - 时序控制：`always @(posedge clk)` 同步状态更新
  - 行为描述：`case(opcode)` 实现指令译码
  - 结构描述：模块化连接数据通路

#### 3. **Verilator 仿真与验证**

Verilator 作为**高性能硬件仿真框架**，在本项目中承担核心验证角色：

图表

代码

下载

- **验证流程**：
  1. 将RISC-V机器码（.hex）加载到仿真内存
  2. 通过C++测试平台注入时钟激励
  3. 监控关键信号（PC值、寄存器写入）
- **核心优势**：
  - 仿真速度比传统工具快50倍以上（实测：1MHz时钟在i7平台实时仿真）
  - 自动检测数据冒险/控制冒险等硬件错误

#### 4. **GTKWave 波形调试**

GTKWave 是**轻量级波形分析工具**，用于可视化验证RISC-V执行过程：

- **关键调试场景**：

  - 流水线冲突分析（RAW/WAW依赖检测）
  - 跳转指令的PC更新时序
  - 内存读写地址对齐验证

- **操作示例**：

  text

  复制

  下载

  ```
  信号分组：/core/IF_stage /core/reg_file  
  关键标记：beq指令的BranchTaken信号跳变  
  时间测量：LW指令的MEM阶段延迟  
  ```

#### 5. **技术协同工作流**

## 三、系统设计
